### Trying basics (2026)

#### Question: Put the following five queries into a single sql file (in order): *yourname-db-lab-02.sql*

- Query #1: show the first 3 book titles and authors where the rating is greater than 4.0.

- Query #2: show the number of books for each format (e.g. paperback, hardcover).

- Query #3: show all book titles where the author is 'Haruki Murakami'.

- Query #4: show the title and pages of the book with the most pages.

- Query #5: show how many books are published between 2020-01-01 and 2023-12-31 (inclusive).

- In ***yourname-db-lab-02.sql***,

  ```sql
  use lab;
  SELECT 'NAME LAB-02-2026' as 'INFO'; /* e.g.) SELECT 'Youngjoon Won LAB-02-2026' as 'INFO'; */
  /* query #1 */
  select ...
  /* query #2 */
  select ...
  /* query #3 */
  select ...
  /* query #4 */
  select ...
  /* query #5 */
  select ...
  ```

**Be very careful!**

- Make sure that your sql file includes 'use lab;' and your name like the above example. So your queries show no errors. 
- Submit it to LMS, sql file only (db-lab-02-question-submission). Check the deadline!
- DO NOT put any space in your submit file name. e.g.) 'young  db-lab-02-2026 .sql' (X). 0 will be given.
- If your query shows any error, 0 will be given.
