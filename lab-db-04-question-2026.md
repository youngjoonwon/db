### SQL Commands (2026)

#### Question: Put the following queries into a single sql file (in order): *yourname-db-lab-04.sql*

- Query #1: Show the author's id who wrote "Red Dog".

- Query #2: Show all book ids written by authors "Claudia Piñeiro" or "Samanta Schweblin"

- Query #3: Show the first 5 books with the minimum rating of each book.

- Query #4: Show the titles of books that have a rating of 1. Use nested queries only. Do not use JOIN yet.

- In ***yourname-db-lab-04.sql***,

  ```sql
  use library;
  SELECT 'NAME LAB-04-2026' as 'INFO'; /* e.g.) SELECT 'Youngjoon Won LAB-04' as 'INFO'; */
  /* query #1 */
  select ...
  /* query #2 */
  select ...
  ```

**Be very careful!**

- Make sure that your sql file includes 'use DBNAME;' and your name like the above example. Use a proper DBNAME. So your queries show no errors. 

- Submit it to LMS, sql file only (db-lab-04-question-submission). Check the deadline!
- DO NOT put any space in your submit file name. e.g.) 'young  db-lab-04-2026 .sql' (X). 0 will be given.
- If your query shows any error, 0 will be given.
